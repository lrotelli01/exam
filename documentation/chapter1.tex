\chapter{Introduction}

This project implements a discrete event simulation of a concurrent database access system using the \textbf{OMNeT++ framework} (Discrete Event Simulator).

\section{Objectives}
The main objective is to evaluate the performance of a multi-client database system by implementing a readers/writers model with concurrency control through mutual exclusion and FCFS (First Come First Served) queueing.

\section{Context}
The simulation analyzes how the system responds to varying loads of read and write operations from $N$ concurrent users accessing $M$ database tables.

\begin{figure}[H]
    \centering
    \begin{tikzpicture}[
        node distance=1cm and 4cm,
        user/.style={circle, draw=blue!60, fill=blue!5, very thick, minimum size=1cm},
        table/.style={rectangle, draw=red!60, fill=red!5, very thick, minimum size=1cm, minimum width=1.5cm},
        arrow/.style={->, >=stealth, thick, color=gray!80}
    ]
        % Users
        \node[user] (u1) {User 1};
        \node[user] (u2) [below=of u1] {User 2};
        \node (udots) [below=0.5cm of u2] {$\vdots$};
        \node[user] (un) [below=0.5cm of udots] {User N};

        % Tables
        \node[table] (t1) [right=of u1] {Table 1};
        \node[table] (t2) [below=of t1] {Table 2};
        \node (tdots) [below=0.5cm of t2] {$\vdots$};
        \node[table] (tm) [below=0.5cm of tdots] {Table M};

        % Connections (simplified mesh)
        \draw[arrow] (u1.east) -- (t1.west);
        \draw[arrow] (u1.east) -- (t2.west);
        \draw[arrow] (u1.east) -- (tm.west);
        
        \draw[arrow] (u2.east) -- (t1.west);
        \draw[arrow] (u2.east) -- (tm.west);
        
        \draw[arrow] (un.east) -- (t1.west);
        \draw[arrow] (un.east) -- (t2.west);
        \draw[arrow] (un.east) -- (tm.west);

    \end{tikzpicture}
    \caption{Simplified interaction schema: N Users accessing M Tables concurrently.}
    \label{fig:users_tables_schema}
\end{figure}
